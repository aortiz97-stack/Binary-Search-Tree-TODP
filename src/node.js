const Node = (value) => {
  const data = value;
  const leftChild = null;
  const rightChild = null;
  const parent = null;

  return {
    data, leftChild, rightChild, parent,
  };
};

export default Node;
